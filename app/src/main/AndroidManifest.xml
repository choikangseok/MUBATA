<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.termproject">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MUBATA"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:label="MUBATA" android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="MUBATA" android:name=".CreateMenu"  android:screenOrientation="portrait"/>
        <activity android:label="MUBATA" android:name=".PlayTabata" android:screenOrientation="portrait"/>

        <service
            android:name=".MusicService"
            android:enabled="true"
            android:exported="true" />

        <activity android:label="MUBATA" android:name=".Movie" android:screenOrientation="portrait"/>
        <activity android:label="MUBATA" android:name=".StopWatch" android:screenOrientation="portrait"/>

        <service
            android:name=".MyBoundService"
            android:enabled="true"
            android:exported="true" />

        <activity android:label="MUBATA" android:name=".Exer_follow" android:screenOrientation="portrait"></activity>
    </application>

</manifest>